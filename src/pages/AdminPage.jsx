/**
 * Dayflow — Admin Page
 * Thin wrapper around AdminDashboard, applying page-level layout.
 */

import AdminDashboard from '../../components/admin/AdminDashboard.jsx';

export default function AdminPage() {
  return <AdminDashboard />;
}
